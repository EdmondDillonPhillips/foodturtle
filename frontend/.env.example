# Frontend — React environment variables
# These are read at BUILD time — must start with REACT_APP_

REACT_APP_API_URL=http://localhost:3001
# This tells React where the backend API lives
# In production this will be your real domain (e.g. https://api.foodturtle.com)

REACT_APP_ENV=development
# Values: development | test | production

REACT_APP_MOCK_PAYMENT=true
# When true: payment is a 90% success mock
# When false: real Stripe (later)